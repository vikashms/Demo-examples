(function () {
    App.Calendar.Models.Month = App.Calendar.Model.extend({
        defaults: {
            days: null
        },
        initialize: function () {
            //var months = new Training.Backbone.ExampleCollection();
            //this.set('months', tasks);
        }

    })
})();